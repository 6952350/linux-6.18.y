// SPDX-License-Identifier: (GPL-2.0+ OR MIT)
/dts-v1/;

/* 包含基础板级定义 */
#include "meson-gxl-s905w-p281.dts"

/* 定义音频路径常量 */
#define AIU_ACODEC_CTRL 2

/ {
	compatible = "amlogic,m302a", "amlogic,s905w", "amlogic,meson-gxl";
	model = "Amlogic Meson GXL (S905L3b) MBH-M302A Box";

	memory@0 {
		device_type = "memory";
		reg = <0x0 0x0 0x0 0x80000000>;
	};

	/* CVBS 视频连接器 */
	cvbs-connector {
		compatible = "composite-video-connector";
		port {
			cvbs_connector_in: endpoint {
				remote-endpoint = <&cvbs_vdac_out>;
			};
		};
	};
};

/* 1. 视频输出连接 */
&cvbs_vdac_port {
	cvbs_vdac_out: endpoint {
		remote-endpoint = <&cvbs_connector_in>;
	};
};

/* 2. 核心修复：通过路径引用声卡节点，增加 AV 音频路由 */
&{/sound} {
	status = "okay";
	audio-routing =
		"AU2 INL", "ACODEC LOLN",
		"AU2 INR", "ACODEC LORN",
		"HDMI INL", "HDMI",
		"HDMI INR", "HDMI";
};

/* 3. 开启内部音频硬件 */
&aiu {
	status = "okay";
};

&acodec {
	status = "okay";
};

/* 4. GPU 核心数修正 (S905L3b 只有 2 个 PP 核心) */
&mali {
	interrupts = <0 160 4>, <0 161 4>, <0 162 4>, <0 163 4>,
		     <0 164 4>, <0 165 4>, <0 166 4>, <0 167 4>;
	interrupt-names = "gp", "gpmmu", "pp", "pmu", "pp0", "ppmmu0", "pp1", "ppmmu1";
};

/* 5. 网络与 eMMC 限制 */
&internal_phy {
	max-speed = <100>;
};

&ethmac {
	status = "okay";
};

&sd_emmc_c {
	status = "okay";
	bus-width = <8>;
	cap-mmc-highspeed;
	mmc-ddr-1_8v;
	mmc-hs200-1_8v;
	max-frequency = <200000000>;
};
